class QuestionSet {
    constructor(name, _id) { // Need to add question list and category
        // device ids contains array of all nicknames of game participants
        this.name = name;
        this._id = _id;
    }
}

export default QuestionSet
