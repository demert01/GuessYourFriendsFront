class Game {
    constructor(deviceIds, joinCode) { // Need to add question list and category
        // device ids contains array of all nicknames of game participants
        this.deviceIds = deviceIds;
        this.joinCode = joinCode;
    }
}

export default Game
